{
  "name": "pcp-workspace",
  "private": true,
  "type": "module",
  "description": "Project Context Protocol (PCP) Monorepo / Workspace",
  "workspaces": [
    "cli"
  ],
  "scripts": {
    "test": "npm --prefix cli test",
    "check": "node cli/bin/pcp.js check",
    "pcp": "node cli/bin/pcp.js"
  }
}
